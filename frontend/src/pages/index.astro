---
import BaseLayout from '../layouts/BaseLayout.astro';
import Icon from '../components/Icons.astro';
import BenefitCard from '../components/BenefitCard.astro';
---

<BaseLayout title="Zellfy - Transforma tu proceso de ventas">
  <!-- Hero -->
  <section class="hero">
    <div class="container">
      <div class="hero-split">
        <div class="hero-content">
          <span class="badge"><Icon name="spark" size={16} color="#00C48C" style="display: inline; margin-right: 0.5rem;" /> Plataforma Todo-en-Uno</span>
          <h1>Transforma tu proceso de ventas en flujo continuo</h1>
          <p class="hero-text">Conecta leads, automatiza seguimiento y cierra más ventas con la plataforma CRM diseñada para equipos modernos.</p>
          <div class="hero-buttons">
            <a href="/contacto" class="btn btn-primary">Prueba Gratis →</a>
            <a href="/como-funciona" class="btn btn-secondary">Ver Demo</a>
          </div>
          <div class="hero-stats">
            <div class="stat">
              <div class="stat-number">500+</div>
              <div class="stat-label">Empresas confían</div>
            </div>
            <div class="stat">
              <div class="stat-number">40%</div>
              <div class="stat-label">Más conversiones</div>
            </div>
            <div class="stat">
              <div class="stat-number">24/7</div>
              <div class="stat-label">Automatización activa</div>
            </div>
          </div>
        </div>
        <div class="hero-image">
          <img 
            src="/dashboard-analytics.jpg" 
            alt="Dashboard de análisis de ventas en tiempo real mostrando métricas de conversión, pipeline de oportunidades y gráficos de rendimiento del equipo comercial" 
            class="main-hero-img"
            loading="eager"
            width="700"
            height="500"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Características -->
  <section class="features">
    <div class="container">
      <div class="text-center mb-4">
        <h2>Todo lo que necesitas para vender más</h2>
        <p>Una plataforma completa que se adapta a tu forma de trabajar</p>
      </div>
      
      <div class="grid grid-cols-3">
        <BenefitCard
          imageSrc="/business-chart.jpg"
          imageAlt="Sistema de captación automática de leads con integraciones a Google Ads, Meta y WhatsApp Business"
          iconName="zap"
          iconGradient="linear-gradient(135deg, #00BFFF 0%, #0099CC 100%)"
          title="Captación Automática"
          description="Conecta con Google Ads, Meta y WhatsApp. Todos tus leads en un solo lugar."
        />

        <BenefitCard
          imageSrc="/crm-dashboard.jpg"
          imageAlt="CRM inteligente con seguimiento 24/7, recordatorios automáticos y priorización de leads con IA"
          iconName="barChart"
          iconGradient="linear-gradient(135deg, #00C48C 0%, #00a076 100%)"
          title="CRM Inteligente"
          description="Seguimiento 24/7, recordatorios automáticos y priorización con IA."
        />

        <BenefitCard
          imageSrc="/social-apps.jpg"
          imageAlt="Mensajería automatizada instantánea vía WhatsApp, email y SMS para atención 24/7"
          iconName="messageCircle"
          iconGradient="linear-gradient(135deg, #00BFFF 0%, #0099CC 100%)"
          title="Respuesta Instantánea"
          description="Mensajería automatizada vía WhatsApp, email y SMS."
        />

        <BenefitCard
          imageSrc="https://images.unsplash.com/photo-1450101499163-c8848c66ca85?w=400&h=250&fit=crop"
          imageAlt="Proceso de cierre rápido con firma electrónica, cotizaciones digitales y validaciones en tiempo real"
          iconName="check"
          iconGradient="linear-gradient(135deg, #00C48C 0%, #00a076 100%)"
          title="Cierre Rápido"
          description="Firma electrónica, cotizaciones digitales y validaciones en tiempo real."
        />

        <BenefitCard
          imageSrc="/automation-workflow.jpg"
          imageAlt="Dashboard en tiempo real con visibilidad total del pipeline y métricas de rendimiento actualizadas al instante"
          iconName="barChart2"
          iconGradient="linear-gradient(135deg, #00BFFF 0%, #0099CC 100%)"
          title="Dashboard en Vivo"
          description="Visibilidad total de tu pipeline. Métricas en tiempo real."
        />

        <BenefitCard
          imageSrc="https://images.unsplash.com/photo-1563013544-824ae1b704d3?w=400&h=250&fit=crop"
          imageAlt="Seguridad empresarial con cumplimiento normativo, encriptación SSL y respaldo automático de datos"
          iconName="lock"
          iconGradient="linear-gradient(135deg, #00C48C 0%, #00a076 100%)"
          title="Seguridad Total"
          description="Cumplimiento normativo, encriptación y respaldo automático."
        />
      </div>
    </div>
  </section>

  <!-- Nueva Sección: Automatización en Acción -->
  <section class="automation-showcase">
    <div class="container">
      <div class="showcase-grid">
        <div class="showcase-content">
          <h2>Automatización que funciona mientras duermes</h2>
          <p class="mb-4">Zellfy captura, califica y responde a tus leads automáticamente. Tu equipo solo se enfoca en cerrar ventas.</p>
          <ul class="feature-list">
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Respuesta inmediata a cada lead</li>
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Calificación inteligente con IA</li>
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Seguimiento automático multicanal</li>
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Recordatorios personalizados</li>
          </ul>
        </div>
        <div class="showcase-image">
          <img 
            src="https://images.unsplash.com/photo-1559028012-481c04fa702d?w=700&h=500&fit=crop" 
            alt="Automatización inteligente de procesos comerciales con IA que responde y califica leads las 24 horas" 
            loading="lazy"
            width="700"
            height="500"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Nueva Sección: Flujo de Trabajo Visual -->
  <section class="workflow-section">
    <div class="container">
      <div class="showcase-grid reverse">
        <div class="showcase-image">
          <img 
            src="https://images.unsplash.com/photo-1542744173-8e7e53415bb0?w=700&h=500&fit=crop" 
            alt="Equipo de ventas trabajando con procesos organizados y dashboards en tiempo real para maximizar productividad" 
            loading="lazy"
            width="700"
            height="500"
          />
        </div>
        <div class="showcase-content">
          <h2>Visibilidad total de tu pipeline</h2>
          <p class="mb-4">Dashboards en tiempo real que muestran cada etapa del proceso. Desde el primer contacto hasta el cierre.</p>
          <ul class="feature-list">
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Métricas actualizadas al instante</li>
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Reportes automáticos personalizados</li>
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Alertas de oportunidades críticas</li>
            <li><Icon name="check" size={20} color="#00C48C" style="display: inline; margin-right: 0.75rem;" />Análisis predictivo con IA</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="final-cta">
    <div class="container">
      <div class="cta-header">
        <span class="cta-badge-center"><Icon name="spark" size={16} color="#00C48C" style="display: inline; margin-right: 0.5rem;" />Demo Gratuita</span>
        <h2 class="cta-title-big">¿Listo para automatizar tu proceso de ventas?</h2>
        <p class="cta-description">Agenda una demo de 20 minutos y descubre cómo Zellfy puede transformar tu negocio</p>
      </div>

      <div class="cta-buttons">
        <a href="/contacto" class="btn btn-primary-large">
          Solicitar Demo Gratis →
        </a>
        <a href="/como-funciona" class="btn btn-secondary-large">
          Ver Cómo Funciona
        </a>
      </div>

      <div class="trust-badges">
        <div class="trust-item">
          <div class="trust-number">500+</div>
          <div class="trust-label">Empresas confían</div>
        </div>
        <div class="trust-item">
          <div class="trust-number">40%</div>
          <div class="trust-label">Más conversiones</div>
        </div>
        <div class="trust-item">
          <div class="trust-number">24/7</div>
          <div class="trust-label">Soporte activo</div>
        </div>
        <div class="trust-item">
          <div class="trust-number">14 días</div>
          <div class="trust-label">Prueba gratis</div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Animaciones Keyframes */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.8;
    }
  }

  @keyframes shimmer {
    0% {
      background-position: -1000px 0;
    }
    100% {
      background-position: 1000px 0;
    }
  }

  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, #ffffff 0%, #f0f9ff 50%, #e0f2fe 100%);
    padding: 6rem 1.5rem 4rem;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(0, 196, 140, 0.1) 0%, transparent 70%);
    animation: float 6s ease-in-out infinite;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -50%;
    left: -50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(0, 191, 255, 0.1) 0%, transparent 70%);
    animation: float 8s ease-in-out infinite reverse;
  }

  .hero-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-content {
    text-align: left;
  }

  .hero-image {
    position: relative;
  }

  .main-hero-img {
    width: 100%;
    height: auto;
    border-radius: 20px;
    box-shadow: 0 25px 80px rgba(0, 0, 0, 0.15);
    animation: float 6s ease-in-out infinite;
    border: 2px solid rgba(0, 191, 255, 0.2);
  }

  .badge {
    display: inline-block;
    background: linear-gradient(135deg, #E8F9F5 0%, #d4f4ec 100%);
    color: #00C48C;
    padding: 0.5rem 1.25rem;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.6s ease-out;
    box-shadow: 0 4px 15px rgba(0, 196, 140, 0.2);
    border: 1px solid rgba(0, 196, 140, 0.2);
    transition: all 0.3s ease;
    text-align: center;
  }

  .badge:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 196, 140, 0.3);
  }

  .hero h1 {
    font-size: 3.5rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: #212529;
    animation: fadeInUp 0.6s ease-out 0.2s both;
    background: linear-gradient(135deg, #212529 0%, #00BFFF 50%, #00C48C 100%);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-text {
    font-size: 1.25rem;
    color: #64748b;
    margin-bottom: 2rem;
    line-height: 1.7;
    animation: fadeInUp 0.6s ease-out 0.4s both;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 3rem;
    animation: fadeInUp 0.6s ease-out 0.6s both;
  }

  .hero-stats {
    display: flex;
    gap: 3rem;
    justify-content: flex-start;
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(226, 232, 240, 0.6);
    animation: fadeInUp 0.6s ease-out 0.8s both;
  }

  .stat {
    transition: transform 0.3s ease;
  }

  .stat:hover {
    transform: translateY(-5px);
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #00BFFF 0%, #00C48C 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.25rem;
  }

  .stat-label {
    font-size: 0.875rem;
    color: #64748b;
  }

  /* Features Section */
  .features {
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    position: relative;
  }

  .card {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    overflow: hidden;
  }

  .card-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-radius: 12px 12px 0 0;
    margin: -1.5rem -1.5rem 1.5rem -1.5rem;
    transition: transform 0.5s ease;
  }

  .card:hover .card-image {
    transform: scale(1.1);
  }

  .card:nth-child(1) { animation-delay: 0.1s; }
  .card:nth-child(2) { animation-delay: 0.2s; }
  .card:nth-child(3) { animation-delay: 0.3s; }
  .card:nth-child(4) { animation-delay: 0.4s; }
  .card:nth-child(5) { animation-delay: 0.5s; }
  .card:nth-child(6) { animation-delay: 0.6s; }

  .card:hover {
    transform: translateY(-10px) scale(1.02);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15) !important;
  }

  .card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #00BFFF, #00C48C);
    opacity: 0;
    transition: opacity 0.3s ease;
    border-radius: 12px 12px 0 0;
  }

  .card:hover::before {
    opacity: 1;
  }

  .card-icon {
    width: 60px;
    height: 60px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
    box-shadow: 0 8px 25px rgba(0, 191, 255, 0.25);
    transition: all 0.3s ease;
    animation: float 3s ease-in-out infinite;
  }

  .card:hover .card-icon {
    transform: scale(1.1) rotate(5deg);
    box-shadow: 0 12px 35px rgba(0, 191, 255, 0.35);
  }

  /* Final CTA Section */
  .final-cta {
    background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
    padding: 6rem 1.5rem;
    text-align: center;
  }

  .cta-header {
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  .cta-badge-center {
    display: inline-block;
    background: linear-gradient(135deg, #E8F9F5 0%, #d4f4ec 100%);
    color: #00C48C;
    padding: 0.5rem 1.5rem;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(0, 196, 140, 0.2);
  }

  .cta-title-big {
    font-size: 3rem;
    line-height: 1.2;
    margin-bottom: 1rem;
    color: #212529;
    font-weight: 700;
  }

  .cta-description {
    font-size: 1.25rem;
    color: #64748b;
    line-height: 1.6;
  }

  .form-wrapper {
    max-width: 700px;
    margin: 0 auto 3rem;
    background: white;
    padding: 2.5rem;
    border-radius: 20px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
    border: 1px solid #e2e8f0;
  }

  .demo-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .form-row-full {
    width: 100%;
  }

  .demo-form input,
  .demo-form select {
    padding: 1rem 1.25rem;
    border: 2px solid #e2e8f0;
    border-radius: 12px;
    font-size: 1rem;
    font-family: var(--font-secondary);
    transition: all 0.3s ease;
    background: white;
  }

  .demo-form input:focus,
  .demo-form select:focus {
    outline: none;
    border-color: #00BFFF;
    box-shadow: 0 0 0 3px rgba(0, 191, 255, 0.1);
  }

  .demo-form select {
    cursor: pointer;
    appearance: none;
    background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="%2364748b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"%3e%3cpolyline points="6 9 12 15 18 9"%3e%3c/polyline%3e%3c/svg%3e');
    background-repeat: no-repeat;
    background-position: right 1rem center;
    background-size: 1.25rem;
    padding-right: 3rem;
  }

  .btn-cta-big {
    background: linear-gradient(135deg, #00C48C 0%, #00a076 100%);
    color: white;
    border: none;
    padding: 1.25rem 2rem;
    border-radius: 12px;
    font-size: 1.125rem;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    transition: all 0.3s ease;
    box-shadow: 0 10px 30px rgba(0, 196, 140, 0.3);
    margin-top: 0.5rem;
  }

  .btn-cta-big:hover {
    transform: translateY(-2px);
    box-shadow: 0 15px 40px rgba(0, 196, 140, 0.4);
  }

  .trust-line {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
    font-size: 0.875rem;
    color: #64748b;
  }

  .trust-badges {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .trust-item {
    text-align: center;
  }

  .trust-number {
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #00BFFF 0%, #00C48C 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .trust-label {
    font-size: 0.875rem;
    color: #64748b;
    font-weight: 500;
  }

  /* Button Enhancements */
  .btn {
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s ease;
  }

  .btn:hover::before {
    left: 100%;
  }

  .btn-primary {
    box-shadow: 0 10px 30px rgba(0, 196, 140, 0.4);
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(0, 196, 140, 0.5);
  }

  .btn-secondary {
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .btn-secondary:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
    background: #e2e8f0;
  }

  /* Automation Showcase */
  .automation-showcase {
    padding: 6rem 1.5rem;
    background: #ffffff;
  }

  .workflow-section {
    padding: 6rem 1.5rem;
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
  }

  .showcase-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .showcase-grid.reverse {
    direction: rtl;
  }

  .showcase-grid.reverse > * {
    direction: ltr;
  }

  .showcase-content h2 {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
    color: #212529;
    line-height: 1.2;
  }

  .showcase-content p {
    font-size: 1.125rem;
    color: #64748b;
    line-height: 1.7;
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 2rem 0;
  }

  .feature-list li {
    padding: 0.75rem 0;
    font-size: 1.125rem;
    color: #212529;
    display: flex;
    align-items: center;
    transition: transform 0.3s ease;
  }

  .feature-list li:hover {
    transform: translateX(8px);
    color: #00C48C;
  }

  .showcase-image {
    position: relative;
  }

  .showcase-image img {
    width: 100%;
    height: auto;
    border-radius: 20px;
    box-shadow: 0 25px 80px rgba(0, 0, 0, 0.15);
    transition: transform 0.5s ease, box-shadow 0.5s ease;
    border: 2px solid rgba(0, 191, 255, 0.1);
  }

  .showcase-image img:hover {
    transform: scale(1.02);
    box-shadow: 0 30px 90px rgba(0, 0, 0, 0.2);
  }

  @media (max-width: 968px) {
    .hero-split {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-content {
      text-align: center;
    }

    .hero-stats {
      justify-content: center;
    }

    .showcase-grid,
    .showcase-grid.reverse {
      grid-template-columns: 1fr;
      gap: 2rem;
      direction: ltr;
    }

    .form-row {
      grid-template-columns: 1fr;
    }

    .trust-badges {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2rem;
    }

    .hero-text {
      font-size: 1.1rem;
    }

    .hero-stats {
      flex-direction: column;
      gap: 1.5rem;
    }

    .hero-buttons {
      flex-direction: column;
    }

    .btn {
      width: 100%;
    }

    .showcase-content h2 {
      font-size: 1.75rem;
    }

    .cta-title-big {
      font-size: 1.75rem;
    }

    .trust-badges {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .form-wrapper {
      padding: 1.5rem;
    }
  }
</style>
